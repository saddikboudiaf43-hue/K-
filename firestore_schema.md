# هيكلية قاعدة بيانات Cloud Firestore لتطبيق K™ 🕌

تم تصميم هيكل قاعدة البيانات وقواعد الأمان لضمان كفاءة عالية في الأداء، وحفظ خصوصية المستخدمين بشكل صارم، مع وضع مستويات صلاحيات دقيقة تتناسب مع أدوار (المستخدم، المشرف، والأدمن) لقرية القدادرة والجمهور العام.

---

## 📂 المجموعات الرئيسية (Root Collections)

### 1️⃣ مجموعة المستخدمين `Users`
تحتوي على الحسابات الشخصية والبيانات التعريفية والصلاحيات.

- **معرف المستند (Document ID):** هو نفسه معرف المستخدم في نظام المصادقة `request.auth.uid` (Firebase Auth UID) لربط المصادقة بقاعدة البيانات بشكل مباشر وسريع.
- **الحقول (Fields):**

| اسم الحقل | نوع البيانات | الوصف | مثال |
| :--- | :--- | :--- | :--- |
| `userId` | String | المعرف الفريد للمستخدم (يتطابق مع UID) | `usr_abc123xyz` |
| `username` | String | الاسم الكامل للمستخدم أو اللقب | `أحمد القدادري` |
| `email` | String | البريد الإلكتروني المسجل | `ahmed@gmail.com` |
| `role` | String | الدور والصلاحيات: `admin` (أدمن)، `supervisor` (مشرف)، `user` (مستخدم) | `user` |
| `village` | String | القرية أو الحي (الافتراضي: القدادرة) | `قرية القدادرة` |
| `avatarUrl` | String | رابط الصورة الشخصية للمستخدم (إن وجد) | `https://lh3.googleusercontent.com/...` |
| `isActive` | Boolean | حالة الحساب (نشط أو محظور) | `true` |
| `createdAt` | Timestamp | تاريخ ووقت إنشاء الحساب | `June 25, 2026 at 10:00:00 AM UTC` |
| `updatedAt` | Timestamp | تاريخ ووقت آخر تحديث للبيانات | `June 25, 2026 at 12:00:00 PM UTC` |

---

### 2️⃣ مجموعة منشورات الحائط `Posts`
تضم كافة الإعلانات، الاستشارات، المبادرات الخيرية، وطلبات الصلح المنشورة على حائط القرية التفاعلي.

- **معرف المستند (Document ID):** معرف عشوائي فريد ومولد تلقائياً (Auto-generated ID).
- **الحقول (Fields):**

| اسم الحقل | نوع البيانات | الوصف | مثال |
| :--- | :--- | :--- | :--- |
| `postId` | String | المعرف الفريد للمنشور | `post_98765` |
| `title` | String | عنوان المنشور أو الموضوع الرئيسي | `مبادرة تنظيف مقبرة القرية` |
| `content` | String | النص التفصيلي للمنشور | `السلام عليكم، ندعوكم للمشاركة في حملة التطوع يوم الجمعة...` |
| `authorId` | String | معرف الكاتب (مرتبط بـ `Users/{userId}`) | `usr_abc123xyz` |
| `authorName` | String | اسم الكاتب عند النشر (لتجنب عمليات Join المكلفة) | `أحمد القدادري` |
| `authorRole` | String | دور الكاتب (مستخدم، مشرف، أدمن) لعرض شارات التميز | `supervisor` |
| `likesCount` | Number | إجمالي عدد الإعجابات بالمنشور | `24` |
| `commentsCount`| Number | إجمالي عدد التعليقات لتحديث الواجهة ديناميكياً | `5` |
| `isPinned` | Boolean | هل المنشور مثبت في أعلى الحائط من قبل الإدارة؟ | `false` |
| `status` | String | حالة المنشور: `PENDING` (قيد المراجعة)، `APPROVED` (مقبول)، `REJECTED` (مرفوض) | `APPROVED` |
| `timestamp` | Timestamp | تاريخ ووقت النشر (الحقل الأساسي للمنشور) | `June 25, 2026 at 2:30:00 PM UTC` |

#### 🔹 المجموعة الفرعية: التعليقات `Posts/{postId}/Comments`
مجموعة فرعية (Subcollection) ملحقة بكل منشور لتخزين التعليقات التفاعلية.

- **الحقول (Fields):**
  - `commentId` (String): معرف التعليق التلقائي.
  - `authorId` (String): معرف صاحب التعليق.
  - `authorName` (String): اسم صاحب التعليق.
  - `content` (String): نص التعليق.
  - `createdAt` (Timestamp): تاريخ كتابة التعليق.

---

### 3️⃣ مجموعة البلاغات والشكاوى `Reports`
تستخدم لإرسال البلاغات حول المنشورات المسيئة، التعليقات الخارجة عن سياق القرية، أو الحسابات المنتحلة للخصوصية والسرية الكاملة.

- **معرف المستند (Document ID):** معرف عشوائي مولد تلقائياً (Auto-generated ID).
- **الحقول (Fields):**

| اسم الحقل | نوع البيانات | الوصف | مثال |
| :--- | :--- | :--- | :--- |
| `reportId` | String | معرف البلاغ | `rep_xyz555` |
| `reporterId` | String | معرف المستخدم الذي أبلغ (الشاكي) | `usr_def456` |
| `reportedByName`| String | اسم المبلغ | `محمد الهادي` |
| `targetType` | String | نوع الهدف المبلغ عنه: `POST` (منشور)، `COMMENT` (تعليق)، `USER` (مستخدم) | `POST` |
| `targetId` | String | معرف الهدف المحدد (Post ID أو Comment ID أو User ID) | `post_98765` |
| `reason` | String | سبب البلاغ بالتفصيل | `محتوى غير لائق ويخالف قيم القرية` |
| `status` | String | حالة البلاغ: `PENDING` (انتظار المعالجة)، `RESOLVED` (تم الحل)، `DISMISSED` (تم الرفض) | `PENDING` |
| `createdAt` | Timestamp | تاريخ الإبلاغ | `June 25, 2026 at 5:00:00 PM UTC` |
| `resolvedBy` | String | معرف المشرف/الأدمن الذي قام بمعالجة الشكوى وتطبيق القرار | `usr_admin789` |

---

### 4️⃣ مجموعة المحتوى الشرعي والتعليمي `Content`
تخزن السور والآيات القرآنية، التفسير الميسر، ومتون ومصادر الفقه المالكي الجزائري (مثل متن ابن عاشر، الرسالة للقيرواني، مختصر خليل) لتسهيل القراءة والمدارسة.

- **معرف المستند (Document ID):** معرف يمثل نوع المحتوى، مثل `quran` أو `fiqh`.
- **الحقول (Fields):**

| اسم الحقل | نوع البيانات | الوصف | مثال |
| :--- | :--- | :--- | :--- |
| `type` | String | تصنيف المحتوى: `QURAN` أو `FIQH` | `FIQH` |
| `title` | String | عنوان الكتاب أو المتن الشرعي | `متن ابن عاشر (المرشد المعين)` |
| `author` | String | اسم المؤلف أو الناظم | `الشيخ عبد الواحد بن عاشر الأنصاري` |
| `era` | String | عصر الوفاة أو الفترة الزمنية للمؤلف | `توفي 1040 هـ` |
| `description` | String | وصف تمهيدي للمرجع أو الكتاب ومحتواه الشرعي | `جامع لأصول الدين والعبادات وتزكية النفس...` |
| `lastUpdated` | Timestamp | تاريخ آخر تحديث أو مراجعة نصوص | `June 20, 2026 at 1:00:00 AM UTC` |

#### 🔹 المجموعات الفرعية للقرآن الكريم: `Content/quran/Surahs`
- **الحقول (Fields):**
  - `surahId` (String): رقم السورة (مثلاً `1` للفاتحة).
  - `name` (String): اسم السورة بالعربية والتشكيل.
  - `type` (String): مكية أو مدنية.
  - `verses` (Array of Maps): مصفوفة تحتوي على الآيات وتفسيرها الميسر ومقاطع الصوت.

#### 🔹 المجموعات الفرعية للفقه: `Content/fiqh/Chapters`
- **الحقول (Fields):**
  - `chapterId` (String): معرف الباب الفقهي (مثلاً `ashir_wudu`).
  - `title` (String): عنوان الباب الفقهي (مثلاً فرائض الوضوء).
  - `content` (String): نص الأبيات النظمية أو العبارات الفقهية بتشكيلها الدقيق.
  - `explanation` (String): الشرح المعتمد والتعليق المالكي الميسر لتسهيل الفهم على الصبيان والناشئة.

---

## 🔒 قواعد الأمان المتطورة (Firestore Security Rules)

تم تطبيق قواعد أمان قوية جداً من خلال ملف `firestore.rules` لحظر الوصول غير المصرح به وضمان حماية بيانات أهالي القرية وسريتها:

1. **الخصوصية التامة للمستخدمين (`Users`):**
   - لا يمكن لأي مستخدم **تعديل حقل دور الصلاحية (`role`)** الخاص به مطلقاً لمنع عمليات الاختراق والترقية الذاتية العشوائية. ترقية الأدوار تتم فقط وحصرياً من خلال الأدمن الأعلى للقرية (`SUPER_ADMIN`).
   - لا يمكن حذف الحسابات إلا بواسطة الأدمن الأعلى لضمان تماسك البيانات.

2. **الرقابة الذكية والتحقق على الحائط (`Posts` & `Comments`):**
   - عند إنشاء أي منشور أو تعليق، يتم التحقق أوتوماتيكياً من أن معرف الكاتب `authorId` يطابق تماماً معرف المستخدم المسجل دخوله حالياً `request.auth.uid`.
   - يمكن لصاحب المنشور تعديل نصه، كما يسمح لباقي المستخدمين بالتفاعل من خلال حقل الإعجابات والتعليقات فقط (`likesCount`, `commentsCount`).
   - يملك المشرفون والأدمن صلاحية مطلقة لتعديل المنشورات (كالتثبيت، أو التعديل، أو الحذف لمكافحة الفتنة وحظر الإساءة).

3. **حماية سرية المشتكين وبلاغات الأمن (`Reports`):**
   - **سرية تامة:** يمنع منعاً باتاً لأي مستخدم عادي قراءة البلاغات والشكاوى المودعة في قاعدة البيانات.
   - يسمح فقط للمستخدم المسجل برفع بلاغ جديد (حيث تفرض القواعد أن يكون هو صاحب البلاغ الفعلي ومطابقة الـ `reportedById`).
   - المشرفون والأدمن وحدهم من يملكون صلاحيات قراءة البلاغات وفحصها وتعديل حالتها للبت فيها.

4. **سلامة المرجعية والنصوص الشرعية (`Content`):**
   - **قراءة عامة ومفتوحة:** نصوص القرآن الكريم وكتب الفقه المالكي مفتوحة للقراءة العامة للجميع (حتى بدون تسجيل دخول) لنشر العلم والتسهيل على الباحثين.
   - **منع العبث:** يمنع منعاً باتاً على أي شخص (بما في ذلك المشرفين) تعديل نصوص السور أو متون الفقه المالكي، وتقتصر صلاحية الكتابة والتعديل على الأدمن الأعلى للقرية بعد التدقيق لضمان حفظ الأمانة العلمية وسلامة المتون الشرعية من العبث.
